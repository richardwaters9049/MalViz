# 2026-07-08 Cleanup Notes

- Added API-specific auth helpers so route handlers return JSON `401`/`403` errors instead of page redirects.
- Tightened upload validation and cleanup paths, including safer byte-size checks and orphaned quarantine-file logging.
- Improved client error messages for upload, scan start, and admin feedback flows.
- Removed unused starter assets and unused package entries for Radix Dialog/Tabs and uuid.
- Added concise comments around queue fallback behavior, worker job claiming, and browser-only theme hydration.
- Fixed a malformed dashboard card class and removed an unnecessary ESLint suppression.

## Follow-up Architecture Pass

- Removed BullMQ from scan startup so PostgreSQL `scan_jobs` is the single scan queue source.
- Added Playwright e2e coverage for upload/scan/report, mobile navigation, and admin feedback.
- Updated project docs to describe Redis as upload rate-limit backing rather than scan queue infrastructure.
